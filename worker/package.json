{
  "name": "blurt-sync-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker + KV backend for Blurt cross-device sync",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv namespace create BLURT_KV"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
