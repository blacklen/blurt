{
  "name": "blurt-sync-server",
  "version": "1.0.0",
  "private": true,
  "description": "Sync backend for Blurt — stores each user's state & chunks in SQLite, keyed by a sync code",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
